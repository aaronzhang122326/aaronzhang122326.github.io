
function setup() {
  createCanvas(windowWidth, windowHeight);
}
function draw() {
  
}
function mousePressed(){
  console.log(round(random(-20, 0)) + " - " + "(-" + round(random(0, 20)) + ") =");
  // if ((round(random(1,2)) === 1)){
  //   console.log(round(random(-20, 0)) + " - " + "(-" + round(random(0, 20)) + ") =");
  // }
  // else {
  //   console.log(round(random(-20, 0)) + " + " + "(-" + round(random(0, 20)) + ") =");
  // }

  //console.log(round(random(0, 20)) + "   " + round(random(0, 20)));
}
  